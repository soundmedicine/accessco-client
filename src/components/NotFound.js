import React from 'react';

const NotFound = () => (
    <div class="alert alert-warning">
  <h4 class="alert-heading">Not found</h4>
  <p class="mb-0">We couldn't find what you were looking for.</p>
</div>
);

export default NotFound;